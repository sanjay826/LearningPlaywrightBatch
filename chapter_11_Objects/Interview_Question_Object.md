
## 1. What is Object and how to use in javascript and also type of Object ?
I Javascript an object is a collection of key-value pairs stored in heap memory. Variable don't 
hold the Object itself they hold a reference (memory address) to it.

-> Objects are collections of key-value pairs. they are fundamental to javascript and used everywhere
## In Object 
Whenever you assign one object to another object it will always copy the reference. 
