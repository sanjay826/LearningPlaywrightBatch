let class ='Hello World';
// class is a reserved keyword in JavaScript, so it cannot be used as an identifier. This is the result in syntax  error
let return =5;
// return is also a reserved keyword in JavaScript , so it connot be used as an identifier. This will also result in syntax  error

let myvariable =10;
// This is valid identifer and can be used without any issues 

// They look like keyword but aren't reserved :
let undefined = '10'; // Work but never do this
let Infinity =15; // Work but never do this