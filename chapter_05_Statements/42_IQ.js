if([]) {
    console.log("true");
}

let response;
// case : 1 
if(response)
{

}

// Case :2
if(response !== null){

}

switch (expression) {
	    case value1:
	        // code block
	        break;       // IMPORTANT: stops fall-through
	    case value2:
	        // code block
	        break;
	    default:
	        // runs if no case matches (optional)
	}

